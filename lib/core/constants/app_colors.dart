import 'package:flutter/material.dart';

class AppColors {
  // --- Ana Yüzeyler (Gölge Konsepti) ---
  static const Color background = Color(0xFF09090A);
  static const Color surface = Color(0xFF141416);
  static const Color surfaceLight = Color(0xFF1A1A1E);
  static const Color border = Color(0xFF1E1E22);
  static const Color borderLight = Color(0xFF2C2C32);

  // --- Metinler ---
  static const Color primaryText = Color(0xFFEDEDEF);
  static const Color secondaryText = Color(0xFF8E8E93);
  static const Color tertiaryText = Color(0xFF5A5A5E);

  // --- Vurgu Renkleri ---
  static const Color accentOlive = Color(0xFF5D7A42);   // Akort yerinde
  static const Color accentAmber = Color(0xFFC5A880);    // Ana vurgu, ibre
  static const Color warningRed = Color(0xFF9E4B4B);     // Hata/Uzak akort

  // --- Usul Ekranı ---
  static const Color usulDum = Color(0xFFD4A574);        // Düm vuruşu (sıcak altın)
  static const Color usulTek = Color(0xFF7A8B6E);        // Tek vuruşu (pastel zeytin)
  static const Color usulInactive = Color(0xFF2A2A2E);   // Pasif vuruş

  // --- Drone Ekranı ---
  static const Color dronePrimary = Color(0xFF4A6B8A);   // Drone aktif (derin mavi-gri)
  static const Color droneWave = Color(0xFF5B8FA8);      // Dalga rengi (açık çelik mavi)
  static const Color droneGlow = Color(0xFF3D5A73);      // Glow efekti

  // --- Makam Aileleri ---
  static const Color familyRast = Color(0xFFC5A880);
  static const Color familyHicaz = Color(0xFFB07A6B);
  static const Color familyUssak = Color(0xFF7A8B6E);
  static const Color familyHuseyni = Color(0xFF6B7B9E);
  static const Color familySegah = Color(0xFF9E7AB0);
  static const Color familyOther = Color(0xFF8E8E93);
}
